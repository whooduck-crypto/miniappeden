# Vercel Deployment Setup

## Проблема
При перезагрузке страницы выдает ошибку: `GET https://miniappeden.vercel.app/tournaments 404 (Not Found)`

## Причина
Когда приложение развернуто на Vercel, переменные окружения `VITE_API_URL` не установлены, поэтому фронтенд пытается получить данные с неправильного URL.

## Решение

### Способ 1: Установить переменные окружения в Vercel (РЕКОМЕНДУЕТСЯ)

1. Перейдите в ваш проект на https://vercel.com
2. Откройте **Settings** → **Environment Variables**
3. Добавьте следующие переменные:

```
VITE_TELEGRAM_BOT_TOKEN = 8364911492:AAEksRYSru-AxjW7Q962t7VfduL6F2OElvA
VITE_TELEGRAM_BOT_USERNAME = testminiappedenbot
VITE_API_URL = https://web-production-b6f80.up.railway.app/api
VITE_ADMIN_IDS = 5116606689
```

4. Нажмите **Save**
5. Перейдите в **Deployments** и нажмите на последний деплой
6. Нажмите **Redeploy** чтобы пересобрать приложение с новыми переменными

### Способ 2: Использовать .env.production файл

Файл `.env.production` уже создан. Убедитесь, что он закоммичен в git:

```bash
git add frontend/.env.production
git commit -m "Add environment configuration for production"
git push
```

## Проверка

После решения ошибка должна исчезнуть, и приложение будет корректно подключаться к backend API на Railway.

### Если ошибка все еще есть:

1. Откройте DevTools (F12) → **Console**
2. Проверьте логи при загрузке страницы
3. Вы должны увидеть сообщение в стиле: `🔌 API URL: https://web-production-b6f80.up.railway.app/api`
4. Если вместо этого видите `http://localhost:3000/api`, значит переменные окружения не применены

## Backend URL

Текущий backend развернут на: `https://web-production-b6f80.up.railway.app`

Если вы развернули backend на другом сервисе (Heroku, Render, etc.), обновите `VITE_API_URL` на правильный URL вашего backend.
