# Режим разработки в браузере ✅

## Что было изменено?

Приложение теперь может работать в браузере **без обязательного входа через Telegram Mini App**.

### Измененные файлы:

1. **`frontend/src/config/telegram.ts`**
   - Добавлена функция `getMockUserId()` - возвращает mock ID пользователя для разработки
   - Добавлена функция `getMockUserInfo()` - возвращает полную информацию mock пользователя
   - Функция `getTelegramUserId()` теперь возвращает `getMockUserId()` если Telegram недоступен
   - Функция `getTelegramUserInfo()` теперь возвращает `getMockUserInfo()` если Telegram недоступен
   - Функция `initTelegramWebApp()` больше не возвращает null, а логирует что работаем в режиме браузера

2. **`frontend/src/config/admin.ts`**
   - Функция `getTelegramUserId()` теперь использует `getMockUserId()` как fallback
   - Добавлена функция `getMockUserId()` - создает и сохраняет mock ID в sessionStorage

## Как это работает?

### Когда Telegram доступен (Mini App):
```
Telegram Mini App → getTelegramUserId() → Возвращает реальный ID из window.Telegram
```

### Когда Telegram НЕ доступен (браузер):
```
Браузер → getTelegramUserId() → Возвращает mock ID (123456789)
```

## Особенности mock пользователя:

- **ID**: 123456789 (сохраняется в sessionStorage для консистентности)
- **Имя**: "Dev User"
- **Username**: "dev_user"
- **Language**: "en"

## Как запустить в браузере?

### 1. Установите зависимости:
```bash
cd frontend
npm install
```

### 2. Запустите dev сервер:
```bash
npm run dev
```

### 3. Откройте в браузере:
```
http://localhost:5173
```

Все будет работать как обычно, но с mock пользователем вместо реального Telegram пользователя!

## Команды:

```bash
# Разработка
npm run dev

# Сборка для production
npm run build

# Предпросмотр сборки
npm run preview

# Линтинг
npm run lint
```

## Логи в консоли браузера:

Вы увидите информационные сообщения:
```
⚠️ 🌐 Телеграм не доступен. Используем Mock пользователя для разработки в браузере
ℹ️ Telegram WebApp не доступен. Работаем в режиме разработки (браузер)
```

Это значит, что всё работает правильно!

## Когда переходить на Telegram Mini App?

Когда вы будете готовы развернуть приложение:

1. Получите реальные Telegram токены
2. Установите переменные окружения в `.env` файл
3. Сделайте Webhook для вашего бота
4. Откройте приложение через Telegram Mini App вместо браузера

## Важно:

- ✅ Все функции приложения работают одинаково с mock пользователем
- ✅ ID сохраняется в sessionStorage (одна сессия браузера)
- ✅ Можно тестировать все страницы и функции
- ✅ Admin функции НЕ будут работать (т.к. mock ID не в списке админов)

Если нужно тестировать админ панель, добавьте 123456789 в `VITE_ADMIN_IDS` в файл `.env`!
