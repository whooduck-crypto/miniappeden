# 🔧 КАК ИСПРАВИТЬ DEPLOYMENT НА NETLIFY

## ❌ ПРОБЛЕМА

Netlify падает с ошибкой при build. Причина - **переменные окружения не установлены**.

---

## ✅ РЕШЕНИЕ (5 ШАГОВ)

### ШАГ 1: Перейти в Netlify Dashboard

1. Откройте https://app.netlify.com
2. Выберите ваш сайт (например "miniappeden")
3. Нажмите **"Site settings"** (шестерёнка)

### ШАГ 2: Перейти в Environment Variables

1. На левой панели найдите **"Build & deploy"**
2. Откройте **"Environment"**
3. Нажмите **"Add environment variables"** (или выберите "Edit variables")

### ШАГ 3: Добавить переменные

**Добавьте эти переменные одну за другой:**

```
VITE_TELEGRAM_BOT_TOKEN = YOUR_TOKEN_HERE
VITE_TELEGRAM_BOT_USERNAME = YOUR_BOT_USERNAME
VITE_ADMIN_IDS = 5116606689
VITE_API_URL = http://localhost:3000/api
```

**Где найти значения:**

- **VITE_TELEGRAM_BOT_TOKEN:** @BotFather → `/mybots` → выбрать бота → **"API Token"**
- **VITE_TELEGRAM_BOT_USERNAME:** Имя бота в Telegram (например "miniappeden_bot")
- **VITE_ADMIN_IDS:** Ваш Telegram ID (5116606689)
- **VITE_API_URL:** Если используете backend, или оставить как есть

### ШАГ 4: Сохранить переменные

1. Нажмите **"Save"** для каждой переменной
2. Должны появиться в списке:
   ```
   ✓ VITE_TELEGRAM_BOT_TOKEN
   ✓ VITE_TELEGRAM_BOT_USERNAME
   ✓ VITE_ADMIN_IDS
   ✓ VITE_API_URL
   ```

### ШАГ 5: Перестроить сайт

1. На главной странице сайта нажмите **"Trigger deploy"**
2. Выберите **"Deploy site"**
3. Ждите сборки... (обычно 1-2 минуты)

**При успешной сборке:** ✅ "Deploy succeeded"

---

## 🐛 DEBUG PANEL

После успешного deployment:

1. **Откройте сайт:** https://miniappeden.netlify.app
2. **В Telegram откройте бота и нажмите "App"**
3. **Нажмите красную кнопку "🐛 DEBUG"** в правом верхнем углу
4. **Проверьте статусы:**

```
Telegram: ✅
WebApp: ✅
User ID: 5116606689
Is Admin: ✅ ДА
Admin IDs: 5116606689
```

**Если видите `Is Admin: ✅ ДА`** → **кнопка ⚙️ Админка видна!** 🎉

---

## 🆘 ЕСЛИ ОШИБКА ОСТАЕТСЯ

### Проверка 1: Логи в Netlify

1. Перейти на сайт в Netlify
2. **"Deploys"** tab
3. Нажать на последний failed deploy
4. Нажать **"Deploy log"**
5. Скопировать ошибку

**Пошлите мне текст ошибки!**

### Проверка 2: Переменные окружения

1. **Site settings** → **Build & deploy** → **Environment**
2. Убедитесь что все 4 переменные там
3. Проверьте нет ли пробелов в значениях

### Проверка 3: Build локально

```bash
# Выполните в папке проекта:
npm run build
```

**Должно выдать:**
```
✓ built in XXXms
```

**Если ошибка локально** - исправьте её сначала!

---

## 📋 ЧЕКЛИСТ

- [ ] Открыли https://app.netlify.com
- [ ] Выбрали свой сайт
- [ ] Нашли "Build & deploy" → "Environment"
- [ ] Добавили все 4 переменные окружения
- [ ] Нажали "Trigger deploy"
- [ ] Ждали успешной сборки (Deploy succeeded ✅)
- [ ] Открыли сайт в Telegram Mini App
- [ ] Нажали "🐛 DEBUG"
- [ ] Видят `Is Admin: ✅ ДА`

**Если все галочки** → **всё работает!** 🎉

---

## 💡 ЕСЛИ ВСЕ ЕЩЕ НЕ РАБОТАЕТ

**Отправьте:**
1. Текст ошибки из Netlify deploy log
2. Скриншот переменных окружения (Environment variables)
3. Результат `npm run build` локально

Тогда смогу точнее помочь! 🆘
