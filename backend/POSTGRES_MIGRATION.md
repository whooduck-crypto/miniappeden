# PostgreSQL Migration Guide

## ✅ Что было сделано

1. **Убран NocoDB** - полностью удалены все зависимости и код работы с NocoDB
2. **Добавлена PostgreSQL** - все данные теперь хранятся в Railway PostgreSQL
3. **Созданы таблицы БД**:
   - `users` - пользователи с балансом, звездами, рейтингом
   - `tournaments` - турниры
   - `tournament_participants` - участники турниров
   - `shop_items` - предметы магазина
   - `user_achievements` - достижения пользователей

## 🚀 Инструкция по запуску

### 1. Установить зависимости
```bash
cd backend
npm install
```

### 2. Инициализировать базу данных
```bash
npm run init-db
```
Это создаст все необходимые таблицы и добавит товары в магазин.

### 3. Запустить сервер
```bash
npm start        # Production
npm run dev      # Development с автоперезагрузкой
```

## 📊 Структура данных

### users
```sql
- telegram_id (PRIMARY KEY)
- username
- first_name
- balance (монеты)
- stars (звезды)
- level
- experience
- wins / losses (для рейтинга)
- game_id / server_id
- owned_items (массив)
- created_at / updated_at
```

### tournaments
```sql
- id (PRIMARY KEY)
- name, description
- start_date, end_date
- max_participants, current_participants
- entry_fee, prize_pool
- status (pending/active/finished)
- created_by (telegram_id)
- created_at / updated_at
```

### tournament_participants
```sql
- id
- tournament_id (FK)
- user_id (FK)
- username
- score
- joined_at
```

### shop_items
```sql
- id
- name
- price
- category
- emoji
```

## 🔄 Миграция старых данных

Если у тебя были данные в NocoDB, нужно было их перенести. Если нужно было сделать это:

1. Экспортировать данные из NocoDB
2. Преобразовать формат
3. Вставить в PostgreSQL

Сейчас база чистая, можешь начать с нуля или написать скрипт миграции.

## 📝 Основные изменения в API

| Функция | До (NocoDB) | После (PostgreSQL) |
|---------|-------------|------------------|
| Создание пользователя | ❌ Fallback на Map | ✅ INSERT в БД |
| Получение профиля | ❌ Map/NocoDB | ✅ SELECT из БД |
| Выдача звезд | ❌ Map iteration | ✅ SQL UPDATE |
| Турниры | ❌ JSON файл | ✅ PostgreSQL |
| Персистентность | ❌ Теряются при перезагрузке | ✅ Сохраняются в БД |

## ✨ Преимущества PostgreSQL

- ✅ Надежное хранилище данных
- ✅ Масштабируемость
- ✅ ACID гарантии
- ✅ Быстрые запросы
- ✅ Транзакции
- ✅ Резервные копии в Railway
- ✅ Нет зависимости от NocoDB API

## 🐛 Если что-то не работает

1. Проверь, что DATABASE_URL правильный в .env
2. Запусти `npm run init-db` заново
3. Посмотри логи: `node server.js`
4. Убедись, что база доступна из Railway dashboard

## 💡 Полезные SQL команды

```bash
# Подключиться к БД
psql postgresql://postgres:PASSWORD@postgres.railway.internal:5432/railway

# Посмотреть таблицы
\dt

# Посмотреть данные
SELECT * FROM users;
SELECT * FROM tournaments;
```
