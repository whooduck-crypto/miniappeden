# Интеграция с Telegram Mini App

Этот документ описывает, как интегрировать приложение с Telegram как Mini App.

## Предварительные требования

- Учетная запись Telegram
- Работающий веб-сервер с HTTPS (обязательно!)
- Node.js и npm установлены
- Базовое понимание работы ботов Telegram

## Шаг 1: Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя вашего мини-приложения
   - Введите юзернейм бота (должен заканчиваться на `_bot`)
4. Скопируйте полученный **токен API** (выглядит как `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

## Шаг 2: Развертывание приложения

### Локально для тестирования

```bash
npm run dev
```

Используйте ngrok или другой инструмент туннелирования для HTTPS:

```bash
ngrok http 5173
```

### На продакшене

1. Соберите приложение:
```bash
npm run build
```

2. Загрузите папку `dist` на ваш сервер (например, Vercel, Netlify, AWS, Heroku)

3. Убедитесь, что приложение доступно по HTTPS URL (например: `https://yourdomain.com/app`)

## Шаг 3: Настройка Mini App в BotFather

### Создание меню с кнопкой Mini App

1. В BotFather отправьте: `/setmenubutton`
2. Выберите ваш бот
3. Отправьте текст для кнопки (например: `🎮 Играть`)
4. Отправьте URL вашего приложения (например: `https://yourdomain.com/app`)

### Альтернативный способ через командную строку

Используйте Telegram Bot API:

```bash
curl -X POST "https://api.telegram.org/bot{YOUR_BOT_TOKEN}/setChatMenuButton" \
  -H "Content-Type: application/json" \
  -d '{
    "menu_button": {
      "type": "web_app",
      "text": "🎮 Играть",
      "web_app": {
        "url": "https://yourdomain.com/app"
      }
    }
  }'
```

Замените `{YOUR_BOT_TOKEN}` на ваш токен.

## Шаг 4: Тестирование

1. Откройте своего бота в Telegram
2. Внизу экрана должна появиться кнопка "🎮 Играть"
3. Нажмите на кнопку - приложение откроется
4. Проверьте все страницы и функциональность

## Шаг 5: Интеграция с Telegram WebApp API (опционально)

Для получения информации о пользователе добавьте в `src/main.tsx`:

```typescript
declare global {
  interface Window {
    Telegram?: any;
  }
}

if (window.Telegram?.WebApp) {
  window.Telegram.WebApp.ready();
  
  const user = window.Telegram.WebApp.initDataUnsafe?.user;
  console.log('User:', user);
  
  // Развернуть приложение на весь экран
  window.Telegram.WebApp.expand();
}
```

## Шаг 6: Использование Telegram API на бэкенде

Для верификации пользователя и сохранения данных создайте бэкенд сервис:

### Пример на Node.js/Express

```javascript
const express = require('express');
const crypto = require('crypto');
const app = express();

const BOT_TOKEN = 'your_bot_token_here';

function verifyTelegramData(initData) {
  const data = new URLSearchParams(initData);
  const hash = data.get('hash');
  data.delete('hash');
  
  const dataCheckString = [...data.entries()]
    .sort()
    .map(([key, value]) => `${key}=${value}`)
    .join('\n');
  
  const secretKey = crypto
    .createHmac('sha256', 'WebAppData')
    .update(BOT_TOKEN)
    .digest();
  
  const calculatedHash = crypto
    .createHmac('sha256', secretKey)
    .update(dataCheckString)
    .digest('hex');
  
  return calculatedHash === hash;
}

app.post('/api/verify', express.json(), (req, res) => {
  const { initData } = req.body;
  
  if (verifyTelegramData(initData)) {
    const data = new URLSearchParams(initData);
    const user = JSON.parse(data.get('user'));
    res.json({ success: true, user });
  } else {
    res.status(401).json({ success: false, error: 'Invalid data' });
  }
});

app.listen(3000, () => console.log('Server running on port 3000'));
```

## Хостинги для развертывания

### Бесплатные варианты:
- **Vercel** - Оптимален для React приложений, HTTPS включен
- **Netlify** - Простая развертка, хороший апстайм
- **GitHub Pages** - Для статических сайтов

### Платные варианты:
- **AWS** - Масштабируемое решение
- **Google Cloud** - Хороший апстайм
- **DigitalOcean** - Доступный VPS
- **Heroku** - Легко развертывать

### Рекомендация для начинающих
Используйте **Vercel** или **Netlify** - они бесплатны и идеально подходят для React приложений.

## Примеры файлов конфигурации

### vercel.json

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
```

### netlify.toml

```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

## Решение проблем

### Приложение не открывается
- Проверьте, что URL доступен и использует HTTPS
- Проверьте, что miniapp был установлен правильно в BotFather
- Перезагрузите приложение

### CORS ошибки
- Убедитесь, что бэкенд правильно настроен на CORS
- Используйте относительные пути в запросах API, где возможно

### Проблемы с мобильной версией
- Тестируйте на реальном мобильном устройстве
- Проверьте масштабирование в index.html `viewport` мета-теге

## Дополнительные ресурсы

- [Официальная документация Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [WebApp API Reference](https://core.telegram.org/bots/webapps#initializing-mini-apps)

## Подсказки для развития

1. **Тестирование** - Используйте DevTools в браузере для тестирования
2. **Localhost testing** - Используйте ngrok для HTTPS на localhost
3. **Размер приложения** - Следите за размером бундла (используйте `npm run build` и анализируйте размер `dist/`)
4. **Кэширование** - Настройте Service Workers для оффлайн поддержки

Удачи в развитии вашего мини-приложения! 🚀
