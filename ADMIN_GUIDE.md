# ⚙️ Документация Админки

## 📋 Быстрый старт

### Кто может получить доступ?

Только администраторы, ID которых добавлены в переменную `VITE_ADMIN_IDS`.

### Как получить доступ?

1. **Добавьте свой Telegram ID в `.env.local`:**

```env
VITE_ADMIN_IDS=123456789,987654321,111222333
```

2. **Перезагрузите браузер**

3. **В приложении появится вкладка "⚙️ Админка"** внизу

---

## 🎮 Функционал админки

### 1️⃣ Создание турниров

**Шаг 1:** Нажмите кнопку **"➕ Создать турнир"**

**Шаг 2:** Заполните форму:

| Поле | Описание | Пример |
|------|---------|--------|
| **Название** | Название турнира | "Быстрый поединок" |
| **Описание** | Краткое описание | "Турнир для новичков" |
| **Дата начала** | Когда начнется турнир | 2026-01-28 |
| **Дата окончания** | Когда закончится | 2026-01-29 |
| **Макс участников** | Максимум игроков | 32 |
| **Вступительный взнос** | Сколько монет нужно за вход | 100 |
| **Призовой фонд** | Сумма призов | 1000 |

**Шаг 3:** Нажмите **"✅ Создать турнир"**

Турнир появится в списке со статусом **"⏱️ Ожидание"**

---

### 2️⃣ Управление турнирами

#### Статусы турниров

- **⏱️ Ожидание** (`pending`) - Турнир создан, но еще не начался
- **🔴 Активный** (`active`) - Турнир в процессе, игроки могут присоединяться
- **✅ Завершен** (`finished`) - Турнир завершен, призы распределены

#### Действия

Для каждого турнира доступны 3 кнопки:

| Кнопка | Описание |
|--------|---------|
| **🏁 Завершить** | Завершить турнир и распределить призы |
| **✏️ Редакт** | Редактировать параметры турнира |
| **🗑️ Удалить** | Удалить турнир (осторожно!) |

---

### 3️⃣ Распределение призов

Когда вы нажимаете **"🏁 Завершить"**, система автоматически:

1. Сортирует участников по очкам
2. Распределяет призы:
   - **🥇 1-е место:** 50% призового фонда
   - **🥈 2-е место:** 30% призового фонда
   - **🥉 3-е место:** 20% призового фонда

3. Добавляет монеты победителям на счет

---

## 🔒 Безопасность

### ВАЖНО! 📛

```env
# ❌ НИКОГДА не коммитьте это в Git!
VITE_ADMIN_IDS=123456789

# ✅ Используйте переменные окружения платформы:
# - Vercel → Project Settings → Environment Variables
# - Netlify → Site settings → Build & deploy → Environment
# - Heroku → Config Vars
```

### Получить свой Telegram ID

Откройте Telegram и найдите бота: **@userinfobot**

Отправьте команду: `/start`

Бот покажет ваш ID (пример: `123456789`)

---

## 📊 API для админки

### Создание турнира

```bash
POST /api/tournaments
Content-Type: application/json

{
  "name": "Турнир",
  "description": "Описание",
  "startDate": "2026-01-28",
  "endDate": "2026-01-29",
  "maxParticipants": 32,
  "entryFee": 100,
  "prizePool": 1000,
  "createdBy": 123456789
}
```

**Ответ:**
```json
{
  "id": 1,
  "name": "Турнир",
  "status": "pending",
  "currentParticipants": 0,
  "participants": [],
  ...
}
```

### Получить все турниры

```bash
GET /api/tournaments
```

Фильтр по статусу:
```bash
GET /api/tournaments?status=active
GET /api/tournaments?status=pending
GET /api/tournaments?status=finished
```

### Завершить турнир

```bash
POST /api/tournaments/1/finish
```

Система автоматически:
- Изменит статус на `finished`
- Распределит призы между участниками
- Добавит монеты на счета

### Удалить турнир

```bash
DELETE /api/tournaments/1
```

⚠️ **Это действие необратимо!**

### Получить результаты турнира

```bash
GET /api/tournaments/1/results
```

**Ответ:**
```json
[
  {
    "userId": 123,
    "username": "Player1",
    "position": 1,
    "score": 100,
    "prize": 500
  },
  ...
]
```

---

## 🎯 Примеры использования

### Пример 1: Создание простого турнира

```tsx
import { useTournamentManagement } from '@/hooks/useTournamentManagement'

function MyAdminComponent() {
  const { createTournament } = useTournamentManagement()

  const handleCreate = async () => {
    const tournament = await createTournament({
      name: 'Мой турнир',
      description: 'Тестовый турнир',
      startDate: '2026-01-28',
      endDate: '2026-01-29',
      maxParticipants: 32,
      entryFee: 100,
      prizePool: 1000,
      createdBy: 123456789,
    })
    console.log('Турнир создан:', tournament)
  }

  return <button onClick={handleCreate}>Создать</button>
}
```

### Пример 2: Завершение турнира

```tsx
import { useTournamentManagement } from '@/hooks/useTournamentManagement'

function FinishButton({ tournamentId }) {
  const { finishTournament } = useTournamentManagement()

  const handleFinish = async () => {
    if (window.confirm('Завершить турнир?')) {
      await finishTournament(tournamentId)
      alert('✅ Турнир завершен, призы распределены!')
    }
  }

  return <button onClick={handleFinish}>🏁 Завершить</button>
}
```

---

## 📱 Интеграция в приложение

### AdminPage компонент

Путь: `src/pages/AdminPage.tsx`

**Возможности:**
- ✅ Полный UI для создания турниров
- ✅ Управление турнирами (редактирование, удаление)
- ✅ Просмотр списка участников
- ✅ Завершение турнира и распределение призов
- ✅ Обработка ошибок

**Автоматическая блокировка:**
- Админка видна только в навигации для админов
- При попытке доступа без прав: показывается сообщение "Доступ запрещен"

---

## 🔐 Проверки доступа

### Функции проверки прав

```tsx
import { 
  isAdmin,
  canAccessAdminPanel,
  canCreateTournament,
  canDeleteTournament
} from '@/config/admin'

// Проверить, является ли пользователь админом
if (isAdmin(userId)) {
  // Показать админку
}

// Проверить, может ли создавать турниры
if (canCreateTournament(userId, userLevel)) {
  // Показать кнопку создания
}

// Проверить, может ли удалить турнир
if (canDeleteTournament(userId)) {
  // Показать кнопку удаления
}
```

---

## 🆘 Частые вопросы

### Q: Как дать доступ третьему лицу?

**A:** Добавьте его Telegram ID в `.env.local`:

```env
VITE_ADMIN_IDS=123456789,987654321,111222333
```

### Q: Почему я не вижу админку?

**A:** Возможные причины:
1. Ваш ID не добавлен в `VITE_ADMIN_IDS`
2. Браузер не перезагружен после изменения `.env.local`
3. Проверьте консоль DevTools на ошибки

### Q: Можно ли восстановить удаленный турнир?

**A:** Нет, удаление необратимо. Используйте с осторожностью!

### Q: Как узнать всех админов?

**A:** Посмотрите в `src/config/admin.ts`:

```tsx
console.log(ADMIN_CONFIG.ADMIN_IDS)
```

### Q: Может ли обычный пользователь создать турнир?

**A:** Да, если его уровень >= 20. Изменить:

```tsx
// В src/config/admin.ts
MIN_LEVEL_TO_CREATE_TOURNAMENT: 20
```

---

## 📚 Дополнительно

- **API документация:** `API_INTEGRATION_GUIDE.md`
- **Интеграция Telegram:** `TELEGRAM_SETUP.md`
- **Примеры кода:** `TELEGRAM_HOOKS_EXAMPLES.tsx`

---

## ✅ Чек-лист админа

- [ ] Получили свой Telegram ID (@userinfobot)
- [ ] Добавили ID в `.env.local`
- [ ] Перезагрузили приложение
- [ ] Видят вкладку "⚙️ Админка" внизу
- [ ] Создали первый турнир
- [ ] Протестировали завершение турнира
- [ ] Проверили распределение призов
- [ ] Готовы управлять турнирами!

🚀 Приложение готово!
