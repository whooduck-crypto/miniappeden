# 🚀 РАЗВЕРТЫВАНИЕ В TELEGRAM MINI APP

## 📋 Полная инструкция по развертыванию

Есть несколько способов развернуть приложение в Telegram. Выберите один из них.

---

## ✅ СПОСОБ 1: VERCEL (Рекомендуется) - 10 минут

### Самый простой способ!

#### Шаг 1: Создать GitHub аккаунт

Если еще нет:
1. Перейти на https://github.com
2. Нажать "Sign up"
3. Создать аккаунт

#### Шаг 2: Создать репозиторий

1. На GitHub нажать **"+"** → **"New repository"**
2. Название: `telegram-games-arena`
3. Описание: `Telegram Mini App with Tournaments`
4. Выбрать **"Public"**
5. Нажать **"Create repository"**

#### Шаг 3: Залить код на GitHub

```bash
# Откройте PowerShell в папке проекта и выполните:

# Инициализировать git
git init

# Добавить файлы
git add .

# Первый commit
git commit -m "Initial commit - Telegram Games Arena"

# Добавить удаленный репозиторий (замените USERNAME)
git remote add origin https://github.com/USERNAME/telegram-games-arena.git

# Залить на GitHub
git branch -M main
git push -u origin main
```

#### Шаг 4: Развернуть на Vercel

1. Перейти на https://vercel.com
2. Нажать **"Sign up"** → выбрать **"GitHub"**
3. Авторизоваться
4. Нажать **"New Project"**
5. Выбрать ваш репозиторий `telegram-games-arena`
6. **Framework Preset:** выбрать **"Vite"**
7. **Environment Variables:** добавить:
   ```
   VITE_TELEGRAM_BOT_TOKEN = ваш_токен
   VITE_ADMIN_IDS = ваш_id
   VITE_API_URL = https://your-backend.com/api
   ```
8. Нажать **"Deploy"**

**✅ Готово!** Приложение будет доступно по адресу вроде:
```
https://telegram-games-arena.vercel.app
```

---

## ✅ СПОСОБ 2: NETLIFY - 10 минут

### Альтернатива Vercel

#### Шаг 1-3: Повторить из Способа 1 (GitHub)

#### Шаг 4: Развернуть на Netlify

1. Перейти на https://netlify.com
2. Нажать **"Sign up"** → выбрать **"GitHub"**
3. Авторизоваться
4. Нажать **"Add new site"** → **"Import an existing project"**
5. Выбрать **"GitHub"**
6. Выбрать ваш репозиторий
7. **Build command:** оставить `npm run build`
8. **Publish directory:** оставить `dist`
9. **Environment:** добавить переменные:
   ```
   VITE_TELEGRAM_BOT_TOKEN
   VITE_ADMIN_IDS
   VITE_API_URL
   ```
10. Нажать **"Deploy site"**

**✅ Готово!** Приложение будет доступно по адресу вроде:
```
https://telegram-games-arena.netlify.app
```

---

## ✅ СПОСОБ 3: СОБСТВЕННЫЙ СЕРВЕР - 30 минут

### Если у вас есть сервер (VPS)

#### Шаг 1: Подготовить сервер

```bash
# На сервере (Ubuntu/Debian)
sudo apt update
sudo apt install nodejs npm git nginx

# Создать папку
mkdir -p /var/www/telegram-games-arena
cd /var/www/telegram-games-arena
```

#### Шаг 2: Клонировать репозиторий

```bash
git clone https://github.com/USERNAME/telegram-games-arena.git .

# Установить зависимости
npm install

# Собрать проект
npm run build
```

#### Шаг 3: Настроить Nginx

```bash
sudo nano /etc/nginx/sites-available/telegram-games-arena
```

Вставить:
```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/telegram-games-arena/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Для backend API
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
    }
}
```

#### Шаг 4: Запустить

```bash
# Активировать конфиг
sudo ln -s /etc/nginx/sites-available/telegram-games-arena /etc/nginx/sites-enabled/

# Перезагрузить Nginx
sudo systemctl reload nginx

# Запустить Backend
node server.js &
```

**✅ Готово!** Приложение доступно на вашем домене

---

## 🔗 ИНТЕГРАЦИЯ С TELEGRAM BOTFATHER

После развертывания нужно обновить URL в @BotFather

### Шаг 1: Откройте @BotFather

1. Telegram → найти **@BotFather**
2. Отправить `/mybots`
3. Выбрать ваш бот
4. Выбрать **"Bot Settings"** → **"Menu Button"** → **"Web App"**

### Шаг 2: Установить URL вашего приложения

Если на Vercel:
```
https://telegram-games-arena.vercel.app
```

Если на Netlify:
```
https://telegram-games-arena.netlify.app
```

Если на своем сервере:
```
https://your-domain.com
```

### Шаг 3: Добавить команду /app

В @BotFather:
1. Выбрать **"Commands"**
2. Добавить команду:
   ```
   app - Open the mini app
   ```

---

## 🌐 РАЗВЕРТЫВАНИЕ BACKEND

Backend нужен для работы турниров, магазина и рейтинга.

### Вариант 1: На Heroku (Бесплатно, с ограничениями)

```bash
# 1. Создать аккаунт на https://heroku.com
# 2. Установить Heroku CLI
# 3. Выполнить:

heroku login
heroku create your-app-name
git push heroku main

# Добавить переменные окружения:
heroku config:set TELEGRAM_BOT_TOKEN=your_token
```

### Вариант 2: На Railway

```bash
# 1. Перейти на https://railway.app
# 2. Подключить GitHub репозиторий
# 3. Railway автоматически запустит server.js
```

### Вариант 3: На своем сервере

```bash
# На сервере в папке проекта:
npm install
node server.js
```

---

## 📱 ОТКРОЙТЕ MINI APP

После всех настроек:

1. Откройте бота в Telegram
2. Нажмите на кнопку **"Menu"** (три линии) внизу
3. Выберите **"App"** или отправьте `/app`
4. **Откроется ваше приложение!** 🎉

---

## ⚙️ ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ

Не забудьте добавить переменные на платформу развертывания:

```env
# Telegram
VITE_TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
VITE_TELEGRAM_BOT_USERNAME=YourBotName
VITE_ADMIN_IDS=YOUR_ID

# Backend
VITE_API_URL=https://your-backend.com/api
VITE_API_KEY=your_secret_key

# На продакшене
NODE_ENV=production
```

### Как добавить на Vercel:
1. Project Settings → Environment Variables
2. Добавить каждую переменную
3. Сохранить

### Как добавить на Netlify:
1. Site settings → Build & deploy → Environment
2. Добавить переменные
3. Сохранить

---

## 🔒 БЕЗОПАСНОСТЬ

### ⚠️ НИКОГДА не коммитьте:
```
❌ .env.local
❌ Токены боту
❌ API ключи
❌ Пароли
```

### ✅ ВСЕГДА используйте:
```
✅ Переменные окружения платформы
✅ .gitignore для .env файлов
✅ Webhook вместо polling для бота
✅ HTTPS для всех URL
```

Добавьте в `.gitignore`:
```
.env.local
.env
node_modules/
dist/
.DS_Store
```

---

## 📊 ЧЕКЛИСТ РАЗВЕРТЫВАНИЯ

### Перед развертыванием:
- [ ] Проект собирается локально (`npm run build`)
- [ ] Все API endpoints работают (`node server.js`)
- [ ] Переменные окружения установлены
- [ ] Git репозиторий создан
- [ ] Код залит на GitHub

### На платформе развертывания:
- [ ] Проект подключен (Vercel/Netlify/собственный сервер)
- [ ] Переменные окружения добавлены
- [ ] Сборка прошла успешно
- [ ] URL приложения получен

### В Telegram:
- [ ] Бот создан через @BotFather
- [ ] URL приложения добавлен в настройки бота
- [ ] Команда `/app` работает
- [ ] Mini App открывается

### Тестирование:
- [ ] Админка видна (если вы админ)
- [ ] Можно создавать турниры
- [ ] Турниры загружаются
- [ ] Магазин работает
- [ ] Рейтинг обновляется

---

## 🆘 ЧАСТЫЕ ПРОБЛЕМЫ

### "Белый экран в Telegram"
**Решение:**
1. Проверьте консоль браузера (F12)
2. Проверьте, что URL правильный
3. Проверьте HTTPS (должен быть HTTPS, не HTTP)

### "API ошибка 404"
**Решение:**
1. Проверьте, что backend запущен
2. Проверьте правильность `VITE_API_URL`
3. Проверьте CORS настройки

### "Админка не видна"
**Решение:**
1. Проверьте, что ваш ID в `VITE_ADMIN_IDS`
2. Перезагрузите приложение (F5)
3. Откройте DevTools и проверьте консоль

### "Турниры не сохраняются"
**Решение:**
1. Проверьте, что backend работает
2. Используйте базу данных вместо Map
3. Проверьте логи сервера

---

## 📚 ДОПОЛНИТЕЛЬНО

- **Документация Vercel:** https://vercel.com/docs
- **Документация Netlify:** https://docs.netlify.com
- **Telegram Bot API:** https://core.telegram.org/bots
- **Telegram Mini Apps:** https://core.telegram.org/bots/webapps
- **Vite документация:** https://vitejs.dev

---

## ✅ ИТОГОВАЯ ИНСТРУКЦИЯ (5 минут)

### Быстро на Vercel:

```bash
# 1. Инициализировать Git
git init
git add .
git commit -m "Initial commit"

# 2. Создать репозиторий на GitHub и залить код
git remote add origin https://github.com/USERNAME/telegram-games-arena.git
git push -u origin main

# 3. На Vercel.com:
# - Подключить GitHub репо
# - Добавить переменные окружения
# - Deploy

# 4. В @BotFather добавить URL вашего приложения

# ✅ Готово!
```

**Приложение будет доступно в Telegram за 5 минут!** 🚀
