# 🔧 Миграция БД: Добавление колонки role

Если вы видите ошибку 500 при присоединении к турниру, это означает, что колонка `role` еще не добавлена в таблицу `tournament_participants`.

## Способ 1: Автоматическая миграция (через API)

После обновления сервера на Railway, запустите эту команду:

```bash
curl -X POST https://web-production-b6f80.up.railway.app/api/migrate/add-role-column
```

Или через PowerShell:
```powershell
Invoke-WebRequest -Uri "https://web-production-b6f80.up.railway.app/api/migrate/add-role-column" -Method POST
```

Ответ должен быть:
```json
{
  "message": "Column role successfully added",
  "status": "success"
}
```

## Способ 2: Вручную через PostgreSQL консоль на Railway

1. Откройте консоль Railway для вашей базы данных PostgreSQL
2. Выполните команду:
```sql
ALTER TABLE tournament_participants ADD COLUMN IF NOT EXISTS role VARCHAR(50);
```

## Способ 3: Локально (если запускаете через init-db.js)

```bash
cd backend
node init-db.js
```

## Проверка

После миграции запустите эту команду чтобы проверить, что колонка добавлена:

```bash
curl "https://web-production-b6f80.up.railway.app/api/tournaments"
```

Попробуйте присоединиться к турниру - ошибка 500 должна исчезнуть.

## ⚠️ Важные замечания

1. **Это миграция для существующих БД** - если вы используете свежую БД с `init-db.js`, колонка уже добавлена
2. **Безопасная операция** - команда использует `IF NOT EXISTS`, поэтому если колонка уже есть, ничего не сломается
3. **Один раз** - нужно запустить только один раз на каждом экземпляре БД

## 🐛 Если миграция не работает

Проверьте:
1. PostgreSQL всё ещё запущен и доступен
2. Вы подключены к правильной БД
3. В консоли вы видите логи о миграции
